# Eval Log — Daily Morning Briefing

Manual evaluation practice for the Milestone 0 Routine (see `CLAUDE.md`). No automated eval harness exists yet — this log is the evidence base for whether/when to build one.

**Note:** routine push notifications are currently unreliable (see "Known limitations" in `CLAUDE.md`) — don't wait for a phone push. Check manually each morning via the Code tab in the mobile app or `claude.ai/code/routines`.

Right after reading each morning's briefing, add a row. Keep entries short — a phrase, not a paragraph.

- **Miss** — something that actually mattered today but wasn't mentioned
- **Wrong** — something inaccurate, hallucinated, or irrelevant that was mentioned
- Leave a cell blank ("—") if there's nothing to note for that column

| Date | Miss | Wrong | Notes |
|------|------|-------|-------|
| 2026-07-28 | — | AI News section | Links were weekly/monthly roundup listicles ("AI Weekly Pulse", "Seven Days Seven Releases", "AI Mega-Update"), not specific single articles — despite the prompt saying to avoid listicles. Tightened step 3 wording same day. |
| 2026-07-29 | Avvoka and Likha-IT/c-fo.com rejections | — | Application-status routine (not the morning briefing) reported both as "awaiting outcome" after their final interviews; user corrected live that both had already rejected him, through a channel Gmail/Calendar never saw (call, portal, or verbal). Root cause: the routine only had Gmail + Calendar as sources, so any status change happening outside those two channels was invisible and got silently stale. Fix same day: created a "Job Applications Tracker" Notion database as a persisted source of truth, and rewrote `prompts/application-status.md` to reconcile against it each run instead of re-deriving everything from Gmail/Calendar from scratch. |
| 2026-07-29 | 46 of 68 total applications since 2026-06-25 (Jun 25–Jul 27) | — | Original application-status scan only looked back ~3-5 days, so it only ever saw the tail end of a 5-week job search — 46 applications going back to 7/6 were invisible until a user-requested full-history deep scan (5 parallel subagents, one per week, over Gmail). Included 5 more rejections it didn't know about (Teoh Capital, Bauer, JustMarkets, plus the Avvoka/Likha-IT ones above) and 2 more live action items with real deadlines (IBM HackerRank test expiring ~8/3, St Trinity video interview). Root cause: no persisted state, so anything older than the lookback window was permanently lost each run — same underlying issue as the row above, now fully backfilled into the Notion tracker. Going forward the reconcile-against-Notion approach (see prior row) should prevent this specific gap from recurring, since the tracker no longer depends on a rolling email lookback to remember old applications. |
| 2026-07-30 | Avvoka — Senior Ruby Developer rejection (the second, 7/24 reapplication) | — | Even with the Notion tracker in place, the routine's own 3-5 day Gmail scan missed this rejection because it landed 6 days after applying — outside the window — and had a neutral subject line ("Application to Senior Ruby Developer in Avvoka"). Only surfaced when the user asked for a manual re-scan and a widened 14-day rejection-keyword sweep matched on body text; even then the preview snippet cut off right before the decisive "we have chosen to move forward with another candidate" line, so the full thread had to be opened to confirm it. Fix same day: rewrote `prompts/application-status.md` step 2 into two passes — the usual recent-activity window, plus a ~14-day resolution sweep for decision language on rows still open — and added a rule to always read full thread bodies rather than trusting snippets before classifying a match. |
| 2026-07-30 | — | — | All good. (Morning briefing) |
| 2026-08-01 | — | "Prisma Residences Association Dues" flagged as recurring | Bill-reminder routine described a calendar event ("Pay Prisma Residences Association Dues (Astra-3017)") as a recurring bill already paid ahead of schedule. User corrected: it was a one-off manual payment, unrelated to the bill sheet's actual recurring "Prisma Assoc Dues" line item. Root cause: the calendar event (created by an earlier run) was itself mislabeled "Association Dues" when its description was really a one-off water-utility disconnection notice — today's run then conflated it with the sheet's similarly-named recurring line by name similarity alone, without checking the underlying source email/notice. |
| 2026-08-04 | Meralco bill (₱10,892.66, due 2026-08-02) sitting unconfirmed as paid or not | — | Bill-reminder routine had no persisted state — same root cause as the 7/29 application-status gap. Each run re-derives everything fresh from the budget sheet + a 14-day Gmail window + Calendar, so a due date that quietly passes without a payment-confirmation email (paid via bank app, cash, or another channel that doesn't email a receipt) has no way to surface as "still open" on the next run; the routine would just stop mentioning it once the due date aged out of relevance, rather than flagging it as overdue. Only caught because the user asked for a manual review right after the routine ran and the gap was reasoned through explicitly, not because the routine itself flagged it. Fix same day: created a "Bills Tracker" Notion database (Payee, Amount, Due Date, Status incl. "Overdue"/"No confirmed date", Recurring/Frequency, Calendar Event Created, Source, Last Confirmed, Notes) seeded with the 4 bills already in flight, and rewrote `prompts/bill-reminder.md` to reconcile against it each run — matching on Payee + Due Date so recurring monthly bills don't collide across months. |
| 2026-08-10 | Entire job-search run — zero postings could be verified | — | Job-search routine's `WebFetch` calls were blocked outright by this session's network egress proxy for every domain tried (~15: LinkedIn, Indeed, Greenhouse, Lever, GitLab, Canonical, Zapier, RemoteOK, Himalayas, arc.dev, jobgether, hnhiring, remoterocketship, automattic.com), so no posting could be opened to confirm freshness, exact remote-location scope, or open-status — only `WebSearch` snippets were available, which were too vague/stale (e.g. one lead dated back to April 2026) to pass the prompt's "exclude rather than include with a caveat" bar. Reported nothing rather than padding the list with unverified leads. Root cause looks like this session's environment network policy, not the routine's instructions — worth checking whether the job-search routine's environment can be given a policy that allows outbound fetches to job boards/ATS domains, otherwise this routine can't function as designed. |
| 2026-08-13 | — | Both reported postings (EverAI Rails role, GitLab Plan:Knowledge role) already unposted/closed | Same root cause as 8/10 — `WebFetch` was again blocked for every external domain tried (job boards, Greenhouse, Ashby, Lever, Himalayas, RemoteOK, etc.), so this run had no way to actually open a listing and confirm open-status. Unlike 8/10, this run did not hold the line: it inferred freshness/eligibility from `WebSearch` snippet text alone (a scraped "posted Aug 10" mention, a scraped "Remote, APAC" region tag) and presented both as reasonably confirmed, flagging only the older GitLab posting's age as uncertain. Both were wrong — the user reported both closed the same day. Confirms the 8/10 finding was right and should have been applied consistently: without `WebFetch`, "confirmed eligible/open" cannot actually be established from search snippets, full stop, and the routine should report nothing (or explicitly label everything as unverified rumor-of-a-posting, not a finding) rather than presenting search-snippet inference as verified. This is now a repeat failure, not a one-off — the network-policy fix for this environment (raised 8/10) is the real blocker; until then, `prompts/job-search.md` step 6 needs to be read as "if `WebFetch` is unavailable, treat as freshness/open-status unconfirmable for every result, no exceptions." |
| 2026-08-14 | Entire job-search run — zero postings could be verified | — | Third consecutive occurrence of the same root cause (8/10, 8/13, now 8/14). `WebFetch` was blocked outright by this session's network egress proxy for every domain tried, including generic non-job-board control tests (en.wikipedia.org, boards.greenhouse.io, jobs.lever.co) — confirming this is a wholesale environment-level egress block, not something specific to job-board domains. `curl "$HTTPS_PROXY/__agentproxy/status"` shows the proxy's `noProxy` allowlist covers only anthropic.com/package-registry domains, nothing web-browsing-relevant — consistent with the environment simply not being configured to allow this routine's core function. Held the line per 8/10's precedent and the prompt's explicit fallback clause: reported nothing rather than inferring from `WebSearch` snippets. This is now a confirmed, persistent (not transient) environment misconfiguration for this routine — the fix belongs at the environment/network-policy level (give the job-search routine's environment outbound access to job boards/ATS domains), not in the prompt. Recommend checking whether this routine's configured environment differs from the one other routines (morning briefing, bill-reminder, application-status) use, since those rely on MCP connectors (Gmail/Calendar/Notion) rather than raw WebFetch and may not be hitting the same block. |
| 2026-08-15 | Entire job-search run — zero postings could be verified | — | Fourth consecutive occurrence (8/10, 8/13, 8/14, now 8/15). `WebFetch` again `EGRESS_BLOCKED` for every domain tried this run (jobs.ashbyhq.com, jobs.lever.co, web3.career, news.ycombinator.com, ycombinator.com, linkedin.com, wellfound.com) — `WebSearch` itself still works fine and surfaced plausible-looking leads (e.g. Cognition "Applied AI Engineer - APAC", a Jobgether "Senior AI Engineer" APAC-tagged listing), but none could be opened to confirm freshness/open-status/exact region scope, so none were reported per the 8/13 lesson. Held the line again. This is now a hard pattern, not noise — 4/4 runs blocked identically since 8/10. Not something a prompt rewrite can fix; needs the environment's network egress policy changed (or this routine moved to an environment/connector path that doesn't depend on raw `WebFetch` to arbitrary job-board domains) before it can ever produce a result again. |
| 2026-08-16 | Entire job-search run — zero postings could be verified | — | Fifth consecutive occurrence (8/10, 8/13, 8/14, 8/15, now 8/16). Ran the proxy's own control test before searching at all this time: `WebFetch` to `en.wikipedia.org` (not a job board, just a generic reachability check) failed with `EGRESS_BLOCKED`, and `curl "$HTTPS_PROXY/__agentproxy/status"` confirms the proxy's `noProxy` allowlist covers only anthropic.com/package-registry/internal-cluster domains — no web-browsing domains at all. Held the line without even attempting `WebSearch` + unverifiable leads, per 8/13's lesson that snippet-only inference produces confirmed-wrong results. 5/5 runs blocked identically since 8/10 — this is a permanent characteristic of whatever environment this routine runs in, not a transient outage. Recommendation unchanged from 8/14: this routine cannot function until it's either given a network egress policy that allows job-board/ATS domains, or rebuilt around an MCP connector that doesn't depend on raw `WebFetch`. Worth considering pausing/disabling this routine's schedule until one of those fixes lands, since 5 consecutive no-op runs suggests the daily cron cost isn't buying anything right now. |
| 2026-08-17 | Entire job-search run — zero postings could be verified | — | Sixth consecutive occurrence (8/10, 8/13, 8/14, 8/15, 8/16, now 8/17). Same control test as 8/16: `WebFetch` to `en.wikipedia.org` still fails `EGRESS_BLOCKED`, and `curl "$HTTPS_PROXY/__agentproxy/status"` shows the identical `noProxy` allowlist (anthropic.com/package-registry/internal-cluster only, no web-browsing domains). Held the line again without attempting `WebSearch`-only leads. This is now a full week of identical failures with no change in the environment — not worth re-running the same control test daily. Recommendation: pause this routine's schedule until the network egress policy is actually fixed (raised 8/10, unresolved 7 days later), and re-enable it manually once a control-domain `WebFetch` succeeds, rather than continuing to burn a daily no-op run. |
| 2026-08-17 (2nd run) | — | — | Egress block resolved: a second job-search run later the same day found `WebFetch` to `en.wikipedia.org` succeeded (first success since 8/9), so ran the full search+verify workflow instead of holding the line. Found 3 confirmed open, region-eligible postings (2x ElevenLabs Full-Stack Engineer, 1x Automattic Experienced Software Engineer) after excluding ~15 other leads: closed/removed postings (EverAI's Ruby on Rails req — closed again, matches the 8/13 finding for the same req; MyShell AI Backend Engineer — 404; Coretas, Notion AI Platform — closed), hard region-locks (GitLab: Canada/UK/Bangalore-only reqs; NIR-YU's Senior Rails Engineer (AI Enablement) — explicitly "Remote - LatAm" despite looking like a strong skills match; Quora/Mento/Close/n8n — US, Canada, or Europe-only), and unverifiable-by-bot-block leads (every `jobs.lever.co` and `jobgether.com` URL tried returned HTTP 403 to `WebFetch` — a site-level bot block, distinct from the now-resolved network-level egress block — so no Lever/Jobgether posting could be confirmed and all were excluded per the prompt's fallback rule, including one appealing Jobgether-sourced Rails+AI lead). Workaround found for Ashby-hosted boards (jobs.ashbyhq.com pages render as an empty JS shell to `WebFetch`) — their public `api.ashbyhq.com/posting-api/job-board/<company>` JSON endpoint returns full posting content including exact remote-eligibility text and works reliably. Recommendation: re-enable this routine's normal schedule now that egress is restored; consider noting the Ashby JSON-API workaround and the Lever/Jobgether 403 gap in `prompts/job-search.md` so future runs don't need to rediscover them. |
| 2026-08-17 (3rd run) | — | — | Third job-search firing the same day, ~35 min after the 2nd run (02:27 UTC → 03:04 UTC). Egress still open. Re-ran full search+verify with a fresh set of queries/sources (Ashby/Greenhouse/Lever/Workable postings not tried in run 2) but landed on the same 3 confirmed-eligible postings as run 2 (2x ElevenLabs Full-Stack Engineer worldwide-remote, 1x Automattic Experienced Software Engineer) — no new Rails-stack or additional non-Rails matches. New leads checked and excluded this run: EverAI's "Full Remote - Worldwide" Rails req (still closed — 3rd confirmation after 8/13, 8/17 run 2), Scale Army Senior Fullstack Engineer Rails/React/Python (closed — live Ashby board now only shows LATAM/Africa/Eastern-Europe-restricted roles), Jitera Senior Rails Engineer "work from anywhere" (Workable board shows zero open jobs — closed), CrewAI Senior Fullstack Rails/Python role (closed; current equivalent "Full-Stack Engineer, Agent Mgmt Platform" is SF on-site only, no sponsorship), Persado React/Ruby (EU-only), Qonto Rails (France/Spain only), Splitero Applied AI Engineer (US-only despite "remote-first" framing), Remote People Senior AI Engineer (Europe-only), Zoom Applied AI Engineer (US-only), Bold Business and Instrumentl (404/403, unverifiable). Confirms run 2's list was a real ceiling, not a partial search — worth flagging that same-day re-runs of this routine are producing duplicate results with real search cost; consider whether the schedule needs de-duplication logic (e.g. skip re-running within N hours of a same-day success) rather than re-discovering the same closed leads each time. |
| 2026-08-18 | — | ElevenLabs Full-Stack Engineer (both variants) reported "confirmed worldwide-remote" on 8/17 runs 2 and 3 | Egress still open (control fetch to en.wikipedia.org succeeded). Re-verified the two ElevenLabs Full-Stack Engineer postings that 8/17 counted as confirmed matches and found the earlier verification was incomplete: the page's marketing copy says "remote and can be executed globally," but a fuller fetch surfaced an actual enumerated "eligible application locations" list — UK, Ireland, Dublin, Spain, US, New York, Sofia, Germany, Poland, Boston, Amsterdam, Bulgaria — with no APAC/Philippines entry anywhere. That's a hard region-lock per the prompt's own rule (a specific country/region list that excludes the Philippines/APAC = exclude, not include with a caveat) that the 8/17 runs missed because they stopped at the "globally" tagline instead of pulling the full eligible-locations list. Corrected: excluded both ElevenLabs postings this run. Root cause is a verification-depth gap, not a tooling failure — worth tightening `prompts/job-search.md` step 6 to require pulling any enumerated eligible-locations list (not just the marketing "remote globally" line) before counting a "remote worldwide"-flavored posting as confirmed. Rechecked ~12 other leads this run (TaskRabbit Rails role, GitLab Ruby/AI Staff Backend role, Kode Health Rails role, Consensys AI Engineer, OpenRouter/Cohere/Misfits & Machines Applied-AI-flavored roles, Remote People AI Engineer, Doist, Checkmate): all closed, US/Europe/Americas-region-locked, or unverifiable, leaving exactly one confirmed match — Automattic's "Experienced Software Engineer" (worldwide remote, no enumerated restriction, PHP/JS stack) — same as 8/17 but now correctly the *only* one, not one of three. Reported the single match plainly rather than padding with the excluded leads. |
| 2026-08-19 | — | — | Egress still open (control fetch succeeded). Broad sweep across Ashby JSON API, Workable, Greenhouse, web3.career and generic search for Rails and AI/LLM/Solutions-Engineer roles. Most leads from search snippets had already moved or closed by the time the live board was fetched (EverAI's Worldwide Rails req gone entirely — board now only shows marketing roles; Brightline, Maximus Health, PAR Technology Rails roles all US-only; Quora's "Senior AI Engineer - Poe" and Cognition's "Applied AI Engineer - APAC" no longer on their live boards; OnTheGoSystems Rails/LLM role returned HTTP 410 Gone; Zapier's "Engineer, Applied AI" is SF-primary/EMEA-secondary, not APAC; Siena AI's "Solutions Engineer" no longer posted, its live "Software Engineer, Platform Experience" role is LatAm-only; CoinMarketCap AI/RAG Engineer listing was ~1y stale and doesn't enumerate Philippines; Remote People AI Engineer is Europe-only) — consistent with the pattern already noted 8/17–8/18 that `WebSearch` snippets lag the live listing and can't be trusted without a fetch. Checked the Notion Job Applications Tracker for duplicates before finalizing. Net result: zero new confirmed matches this run; re-surfaced only Automattic's "Experienced Software Engineer" (verified still open today, worldwide remote, no enumerated restriction, PHP/JS stack), which is not yet in the tracker so still worth surfacing. Third run in a row (8/17 run 3, 8/18, 8/19) landing on this same single posting as the actual ceiling — worth considering whether repeat identical results across runs should be deduplicated/suppressed rather than re-reported daily once the user has seen it once. |

## How to read this later

After ~2 weeks, look for patterns, not one-off complaints:

- Recurring misses of the same *kind* (e.g. always missing follow-up emails) → evidence for a specific fix, not necessarily a whole new backend
- "It forgot something from a few days ago" → evidence for durable memory across runs
- "It mixed up two unrelated things" → evidence for workspace/company isolation
- "I can't tell why it said that" → evidence for provenance tracking

If nothing recurring shows up after a couple weeks, that itself is a useful result: the simple version is working, and building the custom engine isn't justified yet.
